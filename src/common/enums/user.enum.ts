export enum UserRole {
    Child = 'child',
    Monitor = 'monitor',
}

export enum MonitorLevel {
    Aspiring = 'aspiring',
    Official = 'official',
    Super = 'super',
}

export enum LifecycleStatus {
    Active = 'active',
    Archived = 'archived',
}
